TODO list

# remove role, remove user, or
  - remove role, and set role_id field on permission table to null; Then,
  role_id field on database must be a nullable;

  -------------------------------------------------------------

# add modal to back and edit button on forms

-------------------------------------------------------------------

# add Role controller method "set_sessions_array" in model and call it before_action

-------------------------------------------------------------------------------

# set department_id to be null

# flash or @object.erros when use redirec_to

# remove presence true to department_id field at discipline model
